# Render health check — 2026-08-27

The user reported Render deployment of the migration-classification update. Two live polls of `https://edupulse-krcu.onrender.com/api/health/migrations` reached Render but showed the Render application-loading interstitial (`Service waking up` / `Application loading`) rather than an application response. Therefore, no final live migration-table result is available from this deployment window. Earlier live `/api/health/database` checks confirmed the service can reach the configured database. The current application remains usable independently of this optional diagnostic. Password-recovery email delivery remains disabled.
