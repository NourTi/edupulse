# Chat and Receipt Browser Verification

- The Arabic-first parent chat is reachable from the administrator workspace under `اسأل المؤسسة`.
- The chat renders suggested questions, a textarea placeholder, privacy note, and institution-source boundary copy.
- The payment workspace renders the `هوية الإيصال` panel with school-name input, logo picker, local persistence notice, and receipt download/print actions.
- A sample payment PDF action returned the visible success toast `تم تنزيل إيصال PDF عربي منسق.`.
- Automated click-through used the browser preview at desktop width. The production type check, four test files/eight tests, and production build also passed.
