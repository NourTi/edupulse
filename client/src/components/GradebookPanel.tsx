import { Download, FilePenLine, Filter, Plus, Trophy } from "lucide-react";

type StudentRecord = { id: string; name: string; nameAr: string; grade: string; guardian: string; phone: string; level: string; attendance: number; subjects: string[]; status: string };
type Assessment = { studentId: string; level: string; speaking: number; listening: number; reading: number; writing: number; note: string; date: string };

type Props = { students: StudentRecord[]; assessments: Assessment[]; isArabic: boolean };

export const gradebookAverage = (assessment?: Assessment) => assessment ? Math.round((assessment.speaking + assessment.listening + assessment.reading + assessment.writing) / 4) : null;

export function GradebookPanel({ students, assessments, isArabic }: Props) {
  return <div dir={isArabic ? "rtl" : "ltr"} className="-mx-5 -my-5 min-h-[calc(100vh-7rem)] bg-[#f4f7fc] px-4 py-5 text-slate-900 sm:px-6 lg:-mx-8 lg:-my-7 lg:px-8 lg:py-7"><div className="mx-auto max-w-[1480px]">
    <header className="mb-5 flex flex-col gap-4 rounded-2xl border border-slate-200/80 bg-white p-5 shadow-[0_8px_28px_rgba(35,49,82,0.06)] lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-orange-600">{isArabic ? "التقييم الأكاديمي" : "Academic assessment"}</p><h1 className="mt-2 text-3xl font-black tracking-tight text-slate-950 sm:text-4xl">{isArabic ? "دفتر الدرجات" : "Gradebooks"}</h1><p className="mt-2 text-sm text-slate-500">{isArabic ? "تابع التقييمات المعتمدة وملاحظات المعلم حسب الفصل." : "Track approved assessments and teacher notes by term."}</p></div><div className="flex flex-wrap gap-2"><button className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-xs font-bold text-slate-600 hover:bg-slate-50"><Download className="h-4 w-4" />{isArabic ? "تصدير" : "Export"}</button><button className="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-xs font-bold text-white shadow-lg shadow-indigo-200 hover:bg-indigo-700"><Plus className="h-4 w-4" />{isArabic ? "إضافة دفتر" : "Add gradebook"}</button></div></header>
    <section className="overflow-hidden rounded-2xl border border-slate-200/80 bg-white shadow-[0_8px_28px_rgba(35,49,82,0.06)]"><div className="flex items-center justify-between border-b border-slate-100 px-5 pt-4"><div className="flex gap-6 text-xs font-bold text-slate-400"><button className="border-b-2 border-indigo-500 pb-4 text-indigo-600">{isArabic ? "الفصل الأول" : "Term 1"}</button><button className="pb-4 hover:text-slate-700">{isArabic ? "الفصل الثاني" : "Term 2"}</button></div><button className="mb-2 inline-flex items-center gap-2 rounded-lg bg-indigo-50 px-3 py-2 text-xs font-bold text-indigo-700"><Filter className="h-3.5 w-3.5" />{isArabic ? "تصفية" : "Filter"}</button></div><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 p-4"><div><p className="font-black text-slate-800">{isArabic ? "اللغة الإنجليزية · المستوى B2" : "English · Level B2"}</p><p className="mt-1 text-xs text-slate-400">{isArabic ? "التقييمات المعتمدة للطلاب المسجلين" : "Approved assessments for enrolled learners"}</p></div><span className="inline-flex items-center gap-2 rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-black text-emerald-700"><Trophy className="h-3.5 w-3.5" />{assessments.length} {isArabic ? "تقييمات" : "assessments"}</span></div><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-right text-xs"><thead className="bg-slate-50 text-[10px] font-black uppercase tracking-[0.08em] text-slate-400"><tr><th className="px-5 py-4">{isArabic ? "اسم الطالب" : "Student name"}</th><th className="px-4 py-4">{isArabic ? "التحدث · 25%" : "Speaking · 25%"}</th><th className="px-4 py-4">{isArabic ? "الاستماع · 25%" : "Listening · 25%"}</th><th className="px-4 py-4">{isArabic ? "القراءة · 25%" : "Reading · 25%"}</th><th className="px-4 py-4">{isArabic ? "الكتابة · 25%" : "Writing · 25%"}</th><th className="px-4 py-4">{isArabic ? "المعدل" : "Class grade"}</th><th className="px-5 py-4">{isArabic ? "ملاحظات" : "Notes"}</th><th className="px-4 py-4" aria-label="Actions" /></tr></thead><tbody>{students.map(student => { const assessment = assessments.find(item => item.studentId === student.id); const grade = gradebookAverage(assessment); return <tr key={student.id} className="border-t border-slate-100 transition hover:bg-indigo-50/30"><td className="px-5 py-3"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-gradient-to-br from-indigo-500 to-cyan-400 font-black text-white">{student.nameAr.slice(0, 1)}</span><div><p className="font-black text-slate-800">{isArabic ? student.nameAr : student.name}</p><p className="mt-1 text-[10px] text-slate-400">{student.grade}</p></div></div></td>{[assessment?.speaking, assessment?.listening, assessment?.reading, assessment?.writing].map((score, index) => <td key={index} className="px-4 py-3"><span className="inline-flex min-w-16 justify-center rounded-lg border border-slate-200 bg-white px-3 py-2 font-black text-slate-700 shadow-[inset_0_1px_2px_rgba(35,49,82,0.04)]">{score ?? "—"}</span></td>)}<td className="px-4 py-3"><span className={`inline-flex min-w-16 justify-center rounded-lg px-3 py-2 font-black ${grade !== null && grade >= 80 ? "bg-emerald-50 text-emerald-700" : grade !== null ? "bg-amber-50 text-amber-700" : "bg-slate-50 text-slate-400"}`}>{grade ?? "—"}</span></td><td className="max-w-48 px-5 py-3 text-slate-500">{assessment?.note ?? (isArabic ? "لا توجد ملاحظة بعد" : "No note yet")}</td><td className="px-4 py-3"><button className="grid h-8 w-8 place-items-center rounded-lg text-slate-400 hover:bg-slate-100 hover:text-slate-700" aria-label={isArabic ? "تعديل التقييم" : "Edit assessment"}><FilePenLine className="h-4 w-4" /></button></td></tr>; })}</tbody></table></div></section>
    <p className="mt-4 text-xs leading-6 text-slate-400">{isArabic ? "المعدلات المعروضة محسوبة من مهارات CEFR الموجودة في السجل المحلي. لا تُنسب درجات غير موجودة إلى أي طالب." : "Class grades are calculated from CEFR skill records already present in the local workspace. No missing scores are invented."}</p>
  </div></div>;
}
